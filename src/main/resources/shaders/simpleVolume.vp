layout (location = 0) in vec3 position;

out vec3 color;

uniform mat4 MVP;
uniform float zoom;

void main()
{
    color = position;
    gl_Position = vec4(position - 0.5, 0.5);
    gl_Position *= MVP;
    gl_Position.w += zoom;
}
